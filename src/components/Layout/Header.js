import React from 'react';
import { Link } from 'react-router-dom';

export default class Header extends React.Component {
  render() {
    return (
      <header className="logo-header">
        <h1 className="main-title">COINSTATS</h1>
      </header>
    )
  }
}
